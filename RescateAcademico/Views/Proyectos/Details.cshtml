@model RescateAcademico.Models.Proyecto
@{
    ViewData["Title"] = "Detalles del Proyecto";
    Layout = "_Layout";
}

<div class="row mb-4">
    <div class="col">
        <h2 style="color: #6c1d45;"><i class="bi bi-folder"></i> @Model.Titulo</h2>
    </div>
</div>

<div class="row">
    <div class="col-md-8">
        <div class="card border-0 shadow-sm">
            <div class="card-body">
                <h5>Descripción</h5>
                <p>@Model.Descripcion</p>
                <hr />
                <div class="row">
                    <div class="col-md-6">
                        <p><strong>Tipo:</strong> @Model.Tipo</p>
                        <p><strong>Cupo Máximo:</strong> @Model.CupoMaximo</p>
                    </div>
                    <div class="col-md-6">
                        <p><strong>Fecha de Cierre:</strong> @Model.FechaCierre.ToString("dd/MM/yyyy")</p>
                        <p>
                            <strong>Estado:</strong>
                            @if (Model.EstaActivo)
                            {
                                <span class="badge bg-success">Activo</span>
                            }
                            else
                            {
                                <span class="badge bg-secondary">Inactivo</span>
                            }
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="mt-3">
    <a asp-action="Index" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left"></i> Volver
    </a>
</div>
